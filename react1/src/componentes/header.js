 export const Header = (props) => {   // SEMPRE TEM QUE SER MAUISCULOOOOOOO props e usado para guardar um atributo e manipular
    return (
      <header>
        <h1>{props.title}</h1>
        <hr />
        <h1>Outro texto</h1>

      </header>
    )
  }
